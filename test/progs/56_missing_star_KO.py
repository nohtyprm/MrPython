##!FAIL: NotAFunctionError[b]@6:22

def polynomiale_carre(a: float, b: float, c: float, x: float) -> float:
    """Retourne le résultat du polynome carré ax^4 + bx^3 + c pour les valeurs d'appel de a, b, c et x
    """
    return a*(x**4) + b(x**3) + c

